import React from 'react';

// The custom cursor has been disabled to improve performance and remove latency (lag).
// We now rely on the hardware-accelerated native browser cursor.
export const CustomCursor: React.FC = () => {
  return null;
};
